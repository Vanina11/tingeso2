spring:
  application:
    name: autorizacion-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
      retry:
        maxAttempts: 200
        maxInterval: 1000
  datasource:
    url: jdbc:postgresql://localhost:5432/autorizacion
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: update