spring:
  application:
    name: plantilla-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/plantilla
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: update