spring:
  application:
    name: gateway-service
  cloud:
    config:
      enabled: true
      uri: http://${HOSTNAME:localhost}:8081
manage:
    endpoints:
        web:
        exposure:
            include: "*"